services:
  kickgpt:
    build: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file: .env
    environment:
      PORT: 3000
    volumes:
      - kickgpt-data:/app/data
    restart: unless-stopped

volumes:
  kickgpt-data:
